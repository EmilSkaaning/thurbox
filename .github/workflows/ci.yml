name: CI

on:
  pull_request:
    branches: [main]
  # The merge queue re-runs the full suite against the temporary
  # `gh-readonly-queue/*` branch (PR rebased onto main). Without this trigger the
  # required "All Checks" gate never reports on a queued PR and it stalls until
  # the queue's check timeout, so the queue *needs* this to merge anything.
  merge_group:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

# Cancel a superseded run when a new commit is pushed to the same PR, so rapid
# re-pushes don't pile up parallel runs of the (heavy) Rust matrix. Each merge
# queue entry gets a distinct `github.ref`, so queued runs never cancel each
# other.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Classify the PR diff into file-category outputs the other jobs gate on, so a
  # docs-only (or shell-only, website-only, …) PR skips the irrelevant jobs.
  # Job-level `if:` (not workflow-level `paths-ignore:`) is required so the
  # always-run `all-checks` gate still reports a status — a skipped job reports
  # `skipped`, which `all-checks` treats as success.
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust || steps.all.outputs.all }}
      shell: ${{ steps.filter.outputs.shell || steps.all.outputs.all }}
      markdown: ${{ steps.filter.outputs.markdown || steps.all.outputs.all }}
      website: ${{ steps.filter.outputs.website || steps.all.outputs.all }}
      install_sh: ${{ steps.filter.outputs.install_sh || steps.all.outputs.all }}
      install_ps: ${{ steps.filter.outputs.install_ps || steps.all.outputs.all }}
      workflows: ${{ steps.filter.outputs.workflows || steps.all.outputs.all }}
    steps:
      # A merge queue entry already contains exactly the PR's changes; validate
      # the whole suite against it rather than diffing (paths-filter has no PR
      # base on a `merge_group` event). Every filter output falls back to this.
      - id: all
        if: github.event_name == 'merge_group'
        run: echo "all=true" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        if: github.event_name == 'pull_request'
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        id: filter
        if: github.event_name == 'pull_request'
        with:
          # Every filter includes `.github/workflows/ci.yml` so editing the
          # workflow re-runs all jobs — any job's definition may have changed.
          filters: |
            rust:
              - 'src/**'
              - 'tests/**'
              - 'build.rs'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.config/nextest.toml'
              - 'deny.toml'
              - 'sonar-project.properties'
              - '.github/workflows/ci.yml'
            shell:
              - '**/*.sh'
              - '.shellcheckrc'
              - '.github/workflows/ci.yml'
            markdown:
              - '**/*.md'
              - '.rumdl.toml'
              - '.github/workflows/ci.yml'
            website:
              - 'website/**'
              - 'package.json'
              - 'package-lock.json'
              - '.github/workflows/ci.yml'
            install_sh:
              - 'scripts/install.sh'
              - 'scripts/install.bats'
              - '.github/workflows/ci.yml'
            install_ps:
              - 'scripts/install.ps1'
              - 'scripts/install.Tests.ps1'
              - '.github/workflows/ci.yml'
            workflows:
              - '.github/**'
              - 'scripts/dev/lint-workflows.sh'
              - 'scripts/dev/lint-workflows.bats'
              - 'justfile'

  check:
    name: Check
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo check --all

  fmt:
    name: Rustfmt
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo clippy --all-targets --all-features -- -D warnings

  nextest:
    name: Nextest
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: taiki-e/install-action@c44f6b046f1c29ae5918b1e0bfdbb2f1813836fd # v2.84.1
        with:
          tool: cargo-nextest
      - run: cargo nextest run --all --profile ci

  # Native-Windows gate (psmux backend selection, known-folder paths, toast
  # notifications, junction workspaces), split into two parallel jobs so the heavy
  # Windows compile isn't serialized: clippy-driver and rustc don't share
  # artifacts, so a single job ran two back-to-back full builds (~9 min). The
  # separate `cargo check` pass was dropped — `clippy -D warnings` already
  # type-checks, so it covers the compile gate. Each job excludes the build dirs
  # from Defender real-time scanning, the dominant Windows-runner overhead (it
  # scans every .rlib/.exe cargo writes). The local dockur VM
  # (scripts/dev/e2e/windows-vm.sh test-suite) runs the same suite from a
  # cross-built nextest archive; these jobs are the automated mirror.
  windows-clippy:
    name: Windows (clippy)
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Exclude build dirs from Defender real-time scanning
        shell: pwsh
        run: |
          Add-MpPreference -ExclusionPath "${{ github.workspace }}"
          Add-MpPreference -ExclusionPath "$env:USERPROFILE\.cargo"
          Add-MpPreference -ExclusionPath "$env:USERPROFILE\.rustup"
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo clippy --all-targets --all-features -- -D warnings

  windows-test:
    name: Windows (tests)
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Exclude build dirs from Defender real-time scanning
        shell: pwsh
        run: |
          Add-MpPreference -ExclusionPath "${{ github.workspace }}"
          Add-MpPreference -ExclusionPath "$env:USERPROFILE\.cargo"
          Add-MpPreference -ExclusionPath "$env:USERPROFILE\.rustup"
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: taiki-e/install-action@c44f6b046f1c29ae5918b1e0bfdbb2f1813836fd # v2.84.1
        with:
          tool: cargo-nextest
      # --no-fail-fast: surface every native-Windows test failure in one run
      # (the `ci` profile is fail-fast); easier to triage runner-specific issues.
      - run: cargo nextest run --all --profile ci --no-fail-fast

  architecture:
    name: Architecture Rules
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo test --test architecture_rules

  # The v2 plugin host is behind the `plugins` Cargo feature and absent from
  # every other job's build, so without this nothing would compile it. It needs
  # a newer toolchain than the crate's own `rust-version` (mlua declares 1.88)
  # and a C compiler for mlua's vendored Luau sources — cargo cannot express a
  # per-feature MSRV, so the requirement is pinned here instead.
  plugins:
    name: Plugin Host (feature-gated)
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          toolchain: 1.88.0
          components: clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: taiki-e/install-action@c44f6b046f1c29ae5918b1e0bfdbb2f1813836fd # v2.84.1
        with:
          tool: cargo-nextest
      # Type-check the bundled Luau plugins. `docs/v2/` asks for the Luau
      # toolchain before Luau ships; without this the `.luau` files in the
      # binary are checked by nothing.
      - name: Install luau-analyze
        run: |
          set -euo pipefail
          curl -fsSL -o luau.zip \
            https://github.com/luau-lang/luau/releases/download/0.663/luau-ubuntu.zip
          unzip -q luau.zip -d "$HOME/.local/bin"
          chmod +x "$HOME/.local/bin/luau-analyze"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Type-check bundled Luau plugins
        run: ./scripts/dev/lint-luau.sh
      - run: cargo clippy --all-targets --features plugins -- -D warnings
      - run: cargo nextest run --all --features plugins --profile ci
      # Stable builds must not gain the runtime: if mlua ever leaks out of the
      # optional dependency, this is where it surfaces.
      - name: Assert the default build excludes the plugin runtime
        run: |
          if cargo tree --edges normal | grep -q mlua; then
            echo "::error::mlua reached the default dependency tree"
            exit 1
          fi

  tui-smoke:
    name: TUI Smoke Test
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Install tmux
        run: sudo apt-get update && sudo apt-get install -y tmux
      - name: Black-box TUI smoke test
        run: scripts/dev/smoke/tui-smoke.sh

  acceptance:
    name: Acceptance Tests
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - uses: taiki-e/install-action@c44f6b046f1c29ae5918b1e0bfdbb2f1813836fd # v2.84.1
        with:
          tool: cargo-nextest
      - name: Run TUI acceptance (e2e) tests
        run: cargo nextest run --profile ci -E 'test(app::acceptance::)'

  deny:
    name: Cargo Deny
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        checks:
          - advisories
          - bans licenses sources
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25 # v2.1.1
        with:
          command: check ${{ matrix.checks }}
          log-level: warn

  docs:
    name: Documentation
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo doc --no-deps --all-features
        env:
          RUSTDOCFLAGS: "-D warnings"

  conventional-commits:
    name: Conventional Commits
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Check conventional commits
        uses: cocogitto/cocogitto-action@52d0023b4396897f1815648e553db2ab8d782f3a # v4.2.0
        with:
          command: check

  markdown-lint:
    name: Markdown Lint
    needs: changes
    if: needs.changes.outputs.markdown == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Lint markdown files
        uses: rvben/rumdl@479d1a64e3b039b2799329a004a717a2da27cfd2 # v0.2.40
        with:
          report-type: annotations

  install-script:
    name: Install Script Tests
    needs: changes
    if: needs.changes.outputs.install_sh == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install bats-core
        run: sudo apt-get update && sudo apt-get install -y bats
      - name: Run install script tests
        run: bats scripts/install.bats

  install-script-ps:
    name: Install Script Tests (PowerShell)
    needs: changes
    if: needs.changes.outputs.install_ps == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # pwsh is preinstalled on GitHub-hosted runners. The install.ps1 helpers
      # are platform-independent (the tests set PROCESSOR_ARCHITECTURE
      # themselves), so they run on ubuntu rather than needing a windows runner.
      - name: Run install.ps1 helper tests (Pester)
        shell: pwsh
        run: |
          $mod = Get-Module -ListAvailable Pester |
            Sort-Object Version -Descending | Select-Object -First 1
          if (-not $mod -or $mod.Version.Major -lt 5) {
            Install-Module Pester -MinimumVersion 5.0.0 -Force -SkipPublisherCheck -Scope CurrentUser
          }
          Import-Module Pester -MinimumVersion 5.0.0
          $cfg = New-PesterConfiguration
          $cfg.Run.Path = 'scripts/install.Tests.ps1'
          $cfg.Run.Exit = $true
          $cfg.Output.Verbosity = 'Detailed'
          Invoke-Pester -Configuration $cfg

  # The release-delivery invariants: cd.yml releases only from `main` and never
  # with the plugin feature, and (once it exists) nightly.yml publishes to no
  # moderated package channel and marks every release a prerelease. Each is one
  # line from being false, invisible in review, and discovered only by a user who
  # installed the wrong thing — so it gates rather than advises.
  workflow-lint:
    name: Workflow Invariants
    needs: changes
    if: needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install bats-core
        run: sudo apt-get update && sudo apt-get install -y bats
      # The fixture suite first: a broken checker that reports success on every
      # input would otherwise pass the tree check below.
      - name: Test the invariant checker against fixtures
        run: bats scripts/dev/lint-workflows.bats
      - name: Check the release-delivery invariants
        run: ./scripts/dev/lint-workflows.sh

  shellcheck:
    name: ShellCheck
    needs: changes
    if: needs.changes.outputs.shell == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Run shellcheck
        run: git ls-files -z '*.sh' | xargs -0 shellcheck

  website-lint:
    name: Website Lint
    needs: changes
    if: needs.changes.outputs.website == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 26
          cache: npm
      - run: npm ci
      # Page .html sources carry YAML front matter and are partial Eleventy
      # templates, so Prettier/HTMLHint run on the CSS/JS sources and on the
      # built _site output rather than the page sources. lint:website builds
      # the site first, then runs every linter.
      - name: Lint website
        run: npm run lint:website

  sonarqube:
    name: SonarQube
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8.2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      - name: SonarQube Quality Gate check
        uses: SonarSource/sonarqube-quality-gate-action@cf038b0e0cdecfa9e56c198bbb7d21d751d62c3b # v1.2.0
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # Informational only — tracks the release binary size so growth is visible in
  # the PR. Deliberately NOT in `all-checks.needs`, so it never blocks a merge.
  # See docs/PERFORMANCE.md (memory / binary size).
  binary-size:
    name: Binary Size
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Build release binaries
        run: cargo build --release --bins
      - name: Record binary sizes
        run: |
          {
            echo "### Release binary sizes"
            echo ""
            echo "| Binary | Size |"
            echo "| ------ | ---- |"
            for bin in thurbox thurbox-cli; do
              path="target/release/$bin"
              [ -f "$path" ] || continue
              size=$(du -h "$path" | cut -f1)
              bytes=$(stat -c%s "$path")
              echo "| \`$bin\` | $size ($bytes bytes) |"
            done
          } | tee -a "$GITHUB_STEP_SUMMARY"
      - name: Upload binaries as artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-binaries
          path: |
            target/release/thurbox
            target/release/thurbox-cli
          if-no-files-found: warn

  all-checks:
    name: All Checks
    if: always()
    needs:
      - changes
      - check
      - fmt
      - clippy
      - nextest
      - acceptance
      - architecture
      - windows-clippy
      - windows-test
      - tui-smoke
      - deny
      - docs
      - conventional-commits
      - markdown-lint
      - install-script
      - install-script-ps
      - workflow-lint
      - shellcheck
      - website-lint
      - sonarqube
    runs-on: ubuntu-latest
    steps:
      - name: Verify all checks passed
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]] || \
             [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more checks failed or were cancelled"
            exit 1
          fi
          echo "All checks passed"
